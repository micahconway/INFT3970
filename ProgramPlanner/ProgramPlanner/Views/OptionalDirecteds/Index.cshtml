@model IEnumerable<ProgramPlanner.Models.OptionalDirected>

@{
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course.CourseName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DirectedSlot.rule)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DirectedSlot.rule)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.OptionalDirectedID }) |
                @Html.ActionLink("Details", "Details", new { id=item.OptionalDirectedID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.OptionalDirectedID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
