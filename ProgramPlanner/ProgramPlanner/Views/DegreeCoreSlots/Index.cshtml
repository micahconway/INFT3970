@model IEnumerable<ProgramPlanner.Models.DegreeCoreSlot>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.numOfOptions)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.numOfOptions)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DegreeCoreSlotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DegreeCoreSlotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DegreeCoreSlotID })
        </td>
    </tr>
}

</table>
