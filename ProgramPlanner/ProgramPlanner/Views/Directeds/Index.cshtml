@model IEnumerable<ProgramPlanner.Models.Directed>

@{
   
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                Directed
            </th>
            <th>
                Major
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Major.MajorName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.DirectedID }) |
                @Html.ActionLink("Details", "Details", new { id=item.DirectedID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.DirectedID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
