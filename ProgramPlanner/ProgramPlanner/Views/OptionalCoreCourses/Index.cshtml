@model IEnumerable<ProgramPlanner.Models.OptionalCoreCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DegreeCoreSlot.DegreeCoreSlotID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DegreeCoreSlot.DegreeCoreSlotID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OptionalCoreCourseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OptionalCoreCourseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OptionalCoreCourseID })
        </td>
    </tr>
}

</table>
