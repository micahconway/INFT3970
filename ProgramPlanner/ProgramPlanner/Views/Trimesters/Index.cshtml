@model IEnumerable<ProgramPlanner.Models.Trimester>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TrimesterValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TrimesterValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrimesterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrimesterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrimesterID })
        </td>
    </tr>
}

</table>
