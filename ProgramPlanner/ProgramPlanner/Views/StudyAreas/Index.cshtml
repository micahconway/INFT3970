@model IEnumerable<ProgramPlanner.Models.StudyArea>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.University.UniName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudyAreaName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.University.UniName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudyAreaName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudyAreaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudyAreaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudyAreaID })
        </td>
    </tr>
}

</table>
