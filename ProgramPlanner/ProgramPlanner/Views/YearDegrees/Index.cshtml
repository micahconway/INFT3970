@model IEnumerable<ProgramPlanner.Models.YearDegree>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Degree.DegreeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YearDegreeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Degree.DegreeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YearDegreeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YearDegreeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.YearDegreeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YearDegreeID })
        </td>
    </tr>
}

</table>
