@model IEnumerable<ProgramPlanner.Models.ProgramOptionalCoreCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProgramStructure.ProgramStructureID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OptionalCoreID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProgramStructure.ProgramStructureID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OptionalCoreID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProgramOptionalCoreCourseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProgramOptionalCoreCourseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProgramOptionalCoreCourseID })
        </td>
    </tr>
}

</table>
