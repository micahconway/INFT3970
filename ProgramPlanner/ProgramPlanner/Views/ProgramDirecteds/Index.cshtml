@model IEnumerable<ProgramPlanner.Models.ProgramDirected>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProgramStructure.ProgramStructureID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProgramStructure.ProgramStructureID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProgramDirectedID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProgramDirectedID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProgramDirectedID })
        </td>
    </tr>
}

</table>
