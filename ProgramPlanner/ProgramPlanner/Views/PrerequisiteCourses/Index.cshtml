@model IEnumerable<ProgramPlanner.Models.PrerequisiteCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NeededPrereq.NeededPrereqID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NeededPrereqID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NeededPrereq.NeededPrereqID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NeededPrereqID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrerequisiteCourseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrerequisiteCourseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrerequisiteCourseID })
        </td>
    </tr>
}

</table>
