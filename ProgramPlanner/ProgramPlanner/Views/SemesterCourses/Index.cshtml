@model IEnumerable<ProgramPlanner.Models.SemesterCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Semester.SemesterValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Semester.SemesterValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { SemesterID = item.SemesterID, CourseID = item.CourseID }) |
            @Html.ActionLink("Details", "Details", new { SemesterID = item.SemesterID, CourseID = item.CourseID }) |
            @Html.ActionLink("Delete", "Delete", new { SemesterID = item.SemesterID, CourseID = item.CourseID })
        </td>
    </tr>
}

</table>
